[workspace]
resolver = "2"
members = [
    "lease",
    "escrow",
    "reputation",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
strip = true
lto = true
codegen-units = 1
panic = "abort"
